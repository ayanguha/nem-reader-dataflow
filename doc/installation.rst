Installation
======================================

Install and update using `pip`_:

.. code-block:: bash

    pip install -r requirements.txt

If you want to run the tests

.. code-block:: bash

   python test.py
